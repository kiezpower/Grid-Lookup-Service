services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gridlookup
      POSTGRES_PASSWORD: gridlookup
      POSTGRES_DB: gridlookup
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
